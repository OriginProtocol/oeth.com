import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    // @ts-ignore
    Compound: {
      main: "#00d592",
    },
    Aave: {
      main: "#7a26f3",
    },
    Convex: {
      main: "#ff5a5a",
    },
    Morpho: {
      main: "#9bc3e9",
    },
  },
});

export default theme;
